# Rust
/target/
**/*.rs.bk
# Cargo.lock — keep committed for binary apps (reproducible builds)

# Build / release output (dev and release builds — do not commit)
/dist/
/build/
*.exe
*.exe.WebView2/
*.dll
*.pdb
*.so
*.dylib
velocity
velocity.exe

# Dioxus / DX
.dx/
*.wasm

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.project
.settings/
*.sublime-*

# OS
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Local / secrets
gitty-opts.json
.env
.env.*
!.env.example
*.local

# Logs and temp
*.log
*.tmp
*.temp
.cache/

# Installers / archives (local test builds)
*.msi
*.dmg
*.AppImage
# Local release archives (CI uploads to GitHub Releases)
velocity-*.zip
velocity-*.tar.gz